# This machine has no system C compiler/linker (no `cc`, no libc6-dev),
# so we build for the fully self-contained musl target using Rust's
# bundled linker (rust-lld).
[build]
target = "x86_64-unknown-linux-musl"

[target.x86_64-unknown-linux-musl]
linker = "rust-lld"
