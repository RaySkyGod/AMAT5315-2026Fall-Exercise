// Placeholder until the renderer task lands.
pub fn render_video(_dir: &std::path::Path, _out: &std::path::Path) -> anyhow::Result<()> {
    anyhow::bail!("video rendering arrives with the renderer task")
}
